import 'bootstrap/dist/css/bootstrap.min.css';
import type { AppProps } from 'next/app';

function FeelMKRApp({ Component, pageProps }: AppProps) {
  return <Component {...pageProps} />;
}

export default FeelMKRApp;
